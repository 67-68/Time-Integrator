# from ti.core.analysis.detectors.detector import BaseDetector


# class Sequence_direct_detector(BaseDetector):
#     """
#     这个类是一个detector,它被用于生成conditional card.
#     它接受一个config, 它从里面提取出来matcher组后会依次匹配
#     如果matcher不匹配,进度清零,重新开始匹配
#     不支持中间有空隙的匹配
#     """
#     def __init__(self,config):
#         pass